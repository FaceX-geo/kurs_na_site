{
  "schemaVersion": "1.0.0",
  "componentId": "ui.kanban-board",
  "version": "1.0.0",
  "purpose": "Show CRM stage flow with numbered colour-coded columns, white cards and a complete list alternative.",
  "variants": ["kanban", "list"],
  "states": ["ready", "empty", "selected", "move-requested"],
  "inputs": [
    { "name": "columns", "type": "KanbanColumn[]", "required": true },
    { "name": "cards", "type": "KanbanCard[]", "required": true },
    { "name": "view", "type": "kanban|list", "required": false },
    { "name": "selectedId", "type": "string|null", "required": false },
    { "name": "ariaLabel", "type": "string", "required": false },
    { "name": "empty", "type": "ReactNode", "required": false }
  ],
  "events": {
    "openCard": { "payload": "KanbanCard", "description": "Requests card detail without hidden mutation." },
    "moveRequest": { "payload": "{ card: KanbanCard, targetColumnId: string }", "description": "Requests a guarded stage preview." }
  },
  "accessibility": {
    "role": "named board or list",
    "keyboard": "Every card opens by button; stage change uses a labelled native control.",
    "focus": "Preview close restores the originating card or list row.",
    "screenReader": "Column number, name, count and card status are text."
  },
  "motion": { "default": "150ms selection feedback.", "reducedMotion": "No card translation." },
  "invariants": ["List alternative exposes every card and action.", "Drag and drop is never required.", "Move request never mutates before preview and confirmation."]
}
